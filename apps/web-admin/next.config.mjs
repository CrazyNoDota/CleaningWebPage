/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  poweredByHeader: false,
  output: 'standalone',
  basePath: '/admin',
  assetPrefix: '/admin',
};

export default nextConfig;
